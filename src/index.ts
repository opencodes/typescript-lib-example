/**
 * sumTwoNumber
 * @param a - first number
 * @param b - second number
 * @returns 
 * @public
 */
export function sumTwoNumber(a: number, b: number): number {
    const sum = a + b;
    return sum
}